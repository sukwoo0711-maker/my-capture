# inbox — trust

라운드 1 산출: `round1/06-trust.md`. 합산 점수는 3이다. 부분 점수는 로컬 프라이버시 4, 배포 신뢰 2이며 합산 3에 이미 반영되었다. 다른 축은 이 두 부분을 다시 감점하지 않는다.

| claim_id | 주장 | 주 소유 축 | 겹치는 축 | 근거 경로 |
|---|---|---|---|---|
| T01 | 제품 분석 텔레메트리와 캡처 자동 업로드 코드는 없다. OCR 기본 경로는 `Windows.Media.Ocr`이다. | trust | — | `README.md` 개인정보 절; `src/MyCapture.App/App.xaml.cs` (`AddDebug`); `src/MyCapture.Ocr/CaptureOcrService.cs` |
| T02 | 첫 실행은 PP-OCR 가중치를 modelscope.cn에서 받는다. 캡처 본문은 올리지 않고, 저장 전 SHA-256을 본다. 기본 품질 Fast와 별개로 켜져 있다. | trust | — | `src/MyCapture.App/App.xaml.cs` `StartOcrModelDownload`; `src/MyCapture.Ocr/OcrModelStore.cs`; `src/MyCapture.Ocr/OcrModelCatalog.cs` |
| T03 | Real-ESRGAN 다운로드는 Enhanced 품질의 첫 인식에서만 일어난다. | trust | — | `src/MyCapture.Core/Settings/OcrQuality.cs`; `src/MyCapture.Ocr/NeuralOcrEngine.cs` |
| T04 | 자동 색인은 인식 평문을 `index.json`과 `meta.json`의 `OcrText`에 쓴다. `CacheResults == false`여도 자동 색인은 멈추지 않는다. 수동 갤러리 OCR만 그 설정을 본다. | trust | — | `src/MyCapture.App/Ocr/OcrIndexingService.cs`; `src/MyCapture.App/Gallery/GalleryController.cs`; `src/MyCapture.App/Gallery/GalleryWindow.xaml.cs`; `src/MyCapture.Core/Queue/CaptureRecord.cs` |
| T05 | 이미지 기본 보존은 168시간이다. 고정 이미지와 동영상은 그 만료에서 빠진다. 개수 300·용량 2GiB는 둘 다에 적용되고 고정 항목은 용량 퇴거에서 빠진다. 빠른 저장은 큐 밖이다. | trust | functionality (갤러리 보관 정책을 기능으로 채점할 경우, 민감정보 수명은 trust만 감점) | `src/MyCapture.Core/Queue/CaptureRetention.cs`; `src/MyCapture.Core/Queue/CaptureQueue.cs`; `README.md` 저장 위치 절 |
| T06 | 빠른 가리기 탐지 결과는 인식 문자열을 남기지 않는다. 호출자는 좌표만 받고, 디스크에는 사각형 주석만 추가된다. | trust | functionality (가리기 도구 UX·단축키·실행 취소의 주 소유. 평문 비보존만 trust) | `src/MyCapture.Core/Privacy/PrivacyDetection.cs`; `src/MyCapture.App/Ocr/PrivacyRedactionService.cs`; `src/MyCapture.App/Editing/AnnotationEditorController.cs` `AddPrivacyRedactions`; `tests/MyCapture.App.Tests/PrivacyRedactionServiceTests.cs` |
| T07 | 가리기 커밋 뒤에도 `original.png`는 가려지지 않은 픽셀로 남는다. 이후 색인은 `rendered.png`를 우선한다. | trust | functionality (도구가 원본을 파괴하는지의 기능 설명과 겹치면 trust가 잔존 데이터의 주 소유) | `src/MyCapture.App/Editing/CapturePersistenceService.cs`; `src/MyCapture.App/Ocr/OcrIndexingService.cs` `ResolveImagePath`; `src/MyCapture.Core/Queue/CaptureRecord.cs` |
| T08 | 가리기 탐지는 줄 안 패턴(이메일, 한국 전화, 주민번호 형태, Luhn 카드, IPv4, 제한된 비밀 접두)만 본다. 주민번호 체크 자릿수와 IPv6·주소 분류는 없다. 미탐·오탐은 완전 삭제 약속이 아니다. | trust | functionality (탐지 품질을 기능 완성도로 볼 경우. 신뢰 공백으로서의 주 소유는 trust. 악용 절차는 어느 축에도 없음) | `src/MyCapture.Core/Privacy/PrivacyDetector.cs`; `src/MyCapture.Core/Settings/OcrQuality.cs` |
| T09 | 영역 캡처는 커밋 전에 클립보드로 픽셀을 복사한다. 편집 완료·빠른 저장·다른 이름 저장은 평탄화 이미지를 항상 복사한다. `CopyToClipboardOnQuickSave`는 그 경로에서 읽히지 않는다. | trust | — | `src/MyCapture.App/Capture/CaptureOverlayWindow.cs`; `src/MyCapture.App/App.xaml.cs` `OnCaptureSelectionCompletedAsync`; `src/MyCapture.App/Editing/CaptureCommitService.cs` |
| T10 | 프로덕션 로그는 디버그 출력뿐이고 줄바꿈만 이스케이프한다. 화면 OCR 본문을 로그 템플릿에 넣지 않는다. 평문 파일 보존은 인덱스가 담당한다. GitHub 실패 시 응답 본문이 디버그 경고에 올라갈 수 있다. | trust | — | `src/MyCapture.Core/Diagnostics/LogText.cs`; `src/MyCapture.App/App.xaml.cs` `BuildServiceProvider`; `src/MyCapture.App/GitHub/GitHubTokenUploadService.cs`; `src/MyCapture.Core/Storage/AppPaths.cs` |
| T11 | README가 F4로 적은 GitHub 흐름의 코드 기본 단축키는 F9다. 수정자 없는 구 F4만 마이그레이션된다. 사용자가 누를 때만 전송되고, 캡처 자동 업로드는 아니다. | trust | learnability (표와 기본키 불일치의 학습 문제. 전송인지 여부는 trust) | `README.md` 단축키 표; `src/MyCapture.Core/Settings/AppSettings.cs`; `src/MyCapture.Core/Settings/SettingsStore.cs` |
| T12 | 토큰이 없으면 브라우저 UI 자동화가 이슈 초안에 붙여 넣고 댓글은 제출하지 않는다. 기본 URL은 공개 저장소 `issues/new`다. 토큰이 있으면 앱이 PNG를 API로 보내고, 베어러는 정책 요청에만 붙는다. | trust | — | `src/MyCapture.App/App.xaml.cs` `HandleGitHubImageUpload`; `src/MyCapture.App/GitHub/GitHubIssueImageUploadService.cs`; `src/MyCapture.App/GitHub/GitHubCommentUploadAutomation.cs`; `src/MyCapture.App/GitHub/GitHubTokenUploadService.cs`; `src/MyCapture.Core/GitHub/GitHubIssueImageUrl.cs` |
| T13 | GitHub PAT는 CurrentUser DPAPI로 저장되고 설정 내보내기에서 비워진다. 메모리에서는 평문이다. | trust | — | `src/MyCapture.Core/Settings/DpapiSecretConverter.cs`; `src/MyCapture.Core/Settings/AppSettings.cs`; `src/MyCapture.Core/Settings/SettingsStore.cs` `ExportTo` |
| T14 | 토큰 업로드의 두 번째 URL은 업데이터처럼 허용 호스트로 제한되지 않는다. 그 요청에는 토큰 헤더가 없다. | trust | — | `src/MyCapture.App/GitHub/GitHubTokenUploadService.cs`; 대조: `src/MyCapture.App/Updates/GitHubUrlValidator.cs` |
| T15 | 업데이트는 사용자 동작, HTTPS, 고정 API, 리다이렉트 호스트 제한, SHA-256, 실행 직전 재해시가 있다. 체크섬 파일의 게시자 서명은 없다. | trust | — | `src/MyCapture.App/Settings/SettingsWindow.Updates.cs`; `src/MyCapture.App/Updates/GitHubUpdateService.cs`; `src/MyCapture.App/Updates/GitHubUrlValidator.cs`; `src/MyCapture.App/Updates/UpdateInstaller.cs` |
| T16 | 배포 파일은 Authenticode 미서명이다. `package.ps1`은 `Unsigned = true`이고 `SECURITY.md`는 체크섬이 신원 증명이 아니라고 적는다. | trust | — | `SECURITY.md`; `README.md`; `build/package.ps1` |
| T17 | FFmpeg는 번들하지 않는다. 고지된 런타임 라이선스는 .NET/WPF, Extensions, RapidOcrNet, OnnxRuntime, PP-OCR, Real-ESRGAN, Fluent Icons다. | trust | — | `THIRD-PARTY-NOTICES.md`; `Directory.Packages.props`; `src/MyCapture.App/MyCapture.App.csproj`; `src/MyCapture.Ocr/MyCapture.Ocr.csproj` |
| T18 | 단일 인스턴스는 세션 로컬 뮤텍스다. 매니페스트는 `asInvoker`다. 로그인 실행은 HKCU Run만 쓰고 기본 켜짐이다. | trust | convenience (두 번째 실행이 첫 인스턴스를 깨우는 UX를 편의로 채점할 경우. 권한 모델은 trust가 이미 확인) | `src/MyCapture.App/app.manifest`; `src/MyCapture.App/App.xaml.cs`; `src/MyCapture.Platform/Shell/RegistryRunKeyStore.cs`; `src/MyCapture.Core/Settings/AppSettings.cs` `LaunchAtLogin` |
| T19 | ShareX의 업로드 자동화와 MyCapture GitHub 단축키는 제품 목적이 다르다. 후자는 선택 전송이고, 전자가 없다는 이유만으로 신뢰 우위로 채점하지 않는다. | trust | positioning (범위 문장과 겹치면 목적 차이는 positioning, 실제 전송 표면은 trust) | `src/MyCapture.App/GitHub/GitHubIssueImageUploadService.cs`; `docs/competitive-matrix.md`는 초안일 뿐, 전송 여부는 위 코드로 확인 |
| T20 | 드래그 내보내기 복사본은 임시 폴더에 기본 2일 남는다. | trust | — | `src/MyCapture.App/Gallery/GalleryDragExportService.cs` |

performance로 넘기는 주장 없음. 자동 색인의 CPU, 모델 다운로드 시간, 업데이트 다운로드 시간은 이 축의 감점이 아니다.
