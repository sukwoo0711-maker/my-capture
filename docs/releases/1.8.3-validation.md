# 1.8.3 validation and release record

Source verification record. Final source SHA, package hashes and remote completion are recorded in the published release and its linked workflow evidence. Base: `11a69b3c44b0df6b744f4238937489b0c3e88869`.
Task branch: `agent/install-launch-memory-1.8.3`.

Scope: resident launch/Explorer reliability, installer copy coexistence, image memory retention, versioned release. Preserve user data, existing portable migration semantics and ownership checks.

## Evidence and gates

- Baseline Release build and 946 tests passed. Initial sandbox restore failed NU1900 because the NuGet vulnerability feed was inaccessible; the same build succeeded with network access, without disabling audit.
- First combined implementation: Release build and 954 tests passed. Later changes require the final run below.
- Final local Release build: exit 0, zero warnings/errors; Core 376/376 and App 581/581 passed (957 total). Resident installer test, update helper (14 cases), preservation and restart preflight scripts: exit 0/PASS.
- Independent follow-up review found that removing duplicate thumbnail access also removed registration for `IsBroken`. Corrected registration at decode plus hit-only refresh and added an `IsBroken`-only cache regression before the final 957-test run.
- Existing installed MyCapture remains running locally. Native package/installer gates run through the existing isolated Windows workflow, extended to include installer/update scripts; no force-stop of the user's app or Explorer.
- PR #12 initial CodeQL aggregate flagged two cleanup paths in the new decode test. Replaced ad-hoc temp-directory deletion with `OwnedTestDirectory` and tested input unlocking through exclusive reopen. No security alert was dismissed or suppressed; exact-head CI is rerun.
- First hosted package run `34329800900` failed 2/7 diagnostics: settings still expected the old Run command; recording's cold first capture/write consumed its entire timed sample and produced one unplayable frame. Settings now asserts `--background`; recording waits for the first successful encoded frame before its unchanged sampling interval, requires at least two frames, and retains all file/reopen/dimension/stop assertions. Failed initial reports remain under `artifacts/validation/1.8.3/first-hosted`; they are not passes.
- PR #12 merged to `3e98ffda90557a7957bc9e1c6f354a6f0daa3217`; tree matched reviewed head `63d9b5d3b9b69940d6cf5b326fa40b05f6e7cfdf`. Main CI/CodeQL passed. Hosted run `34331251701` passed 7/7 native diagnostics, installer hostile 18 cases/11 invocations, resident guard, 14 update helper cases, preservation and restart preflight assertions. The workflow still failed because negative-case child exit status leaked through the shared PowerShell host. It is not recorded as an overall pass. The follow-up invokes each harness in a separate process with checked exit status, preserving every failure assertion.
- Memory implementation was delegated to `memory_review`; integrated C# changes independently reviewed by `independent_review`, with no blocking finding. Lead inspected the patch and runs validation.
- AGY run `197483b5-5e51-47f2-b5a9-12d7a0cf74b4` is NOT a passing review: wrapper reported `scope_violation` because source changed during its read-only run. No worker patch was integrated; no permission broadening/retry.

Required final commands (exit zero and reports required):

```powershell
./build/build.ps1 -Configuration Release -Test
./build/tests/installer-resident-tests.ps1
./build/tests/update-helper-tests.ps1
./build/tests/update-preservation-tests.ps1
./build/tests/update-restart-preflight-tests.ps1
./build/package.ps1 -Version 1.8.3
./build/run-packaged-self-tests.ps1 -PublishRoot artifacts/release/1.8.3/publish-win-x64
./build/tests/installer-hostile-tests.ps1 -Version 1.8.3 -ArtifactRoot artifacts/release/1.8.3
```

Use the [1.8.0 provenance runbook](1.8.0-validation.md): exact checked PR head, verified integration to main, clean main packaging, manifest source SHA and hashes, same-source tag, six required assets and published asset verification. Keep generated reports and final source/hash evidence under `artifacts/validation`.

## Precision, gallery and reduced export follow-up

- User added these requirements before the v1.8.3 tag/publication. Earlier main package run `34332732042` passed at `07867172286ce68966dde2c16d8e0c0f6acfb512`, but that artifact is superseded and must not be published for the new feature source.
- AGY Analyze run `9869de24-a5c2-4130-9da0-206fef42004f` completed successfully at clean base `0786717`, with no edits, denials or scope violations. Its code-path findings were checked; unmeasured latency estimates and PNG quantization/downscaling proposals were not adopted.
- `memory_review` authored shared precision input and asynchronous recording selection; `export_implementation` authored optional reduced export. Lead authored gallery integration. `independent_review` found one P2: concurrent full-size gallery decodes across different records. A shared single-flight guard corrected it and follow-up review found no remaining blocking issue.
- Initial and final integrated Release builds passed with zero warnings/errors and 983 tests (Core 376, App 607). The final run includes the shared full-size image single-flight guard.
- Native synthetic WPF UX run `MyCapture-ux-review-jatqly4u.umb` passed. Actual tile-origin wheel moved the outer viewer 48 DIP, inner scroll viewer was absent, pixel scrolling enabled, and offscreen rows stayed virtualized. Gallery and reduced-export renders at 96/144/192 raster DPI were generated; lead inspected the normal export and compact gallery images. Reduced-motion override was enabled. All connected monitors reported 96 DPI; raster density is not a native mixed-DPI test.
- Export measures lossless flattened PNG bytes versus actual output bytes, preserves dimensions, and uses bounded JPEG quality search for opaque images. True/indexed alpha retains original PNG, no larger output replaces the original, stale preview work is cancelled, and shell drop starts only from a prepared file. Normal saves/drag remain original quality.
- Final exact-main package/native run, source SHA, artifact hashes and release verification belong in the generated release evidence. No earlier package is a substitute for the final feature source.

## Remaining limits

- Broadcast reception is verified using a unique registered broadcast and hidden HWND enumeration; shell self-test separately verifies tray re-registration. Tests do not kill/restart the user's Explorer process.
- Crop ownership, long-edge decode, cache budget and hidden binding release have regressions. No uncontrolled working-set percentage reduction is claimed. Active editor/recording/pin image memory is intentionally retained.
- Review identified annotation insertion/undo assets retained for the active editor session. Safe reclamation requires undo/redo reachability tracking and remains follow-up work; it is not reported fixed.
- Existing portable-to-installed update behavior is explicit and preserved. Do not infer ownership from filenames or PE ProductName to overwrite an unmanifested user directory.
- Multi-DPI interactive launch/focus and the user's original machine-specific hang remain manual validation limits.
- Recording diagnostic sampling now covers two multi-frame sessions after the first successful write. It does not prove the original cold-start/early-stop one-frame clip playable; that observed short-recording limitation remains unresolved. The 15-second readiness deadline is not a bound on codec finalization; the outer packaged runner retains its process timeout.
