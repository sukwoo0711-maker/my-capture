# MyCapture 2.1.0

Status: implementing approved UI/UX direction. User accepted the recommendation of direction 02 light three-pane gallery + direction 03 dark precision video editor with one teal brand system. User requested fixes against 2.0.0 and publication of 2.1.0. No 2.1.0 tag, package or release has been published.

## Verified baseline

On 2026-09-13, origin/main and v2.0.0 both resolve to c10b6d5e1e7f2c9a8a2799dd1193464ae2a0c69c (tree 9bd5a6bffc213862b2805a644a2b24b8acb337a1). Version metadata is 2.0.0/2.0.0.0. Published release-manifest.json identifies that same commit and SourceTreeClean=true; its SHA-256 matches GitHub asset digest 929ad9fe958952a8285b237a107366092650e98d1fd6bae83b8cad3e56e99d22. No remote history rewrite or main source correction was necessary. Local main was fast-forwarded before creating fix/editor-workflow-2.1.0.

## Outcome and boundaries

- Date-group selection toggles: all selected means clear this group, otherwise select this group. Other groups retain selection.
- Library edit action has a recognizable edit icon with tooltip/accessibility text.
- Retention text explicitly describes time until automatic deletion, including pinned/non-expiring states.
- Review Ctrl+Shift+X recording selection placement, capture preparation and input behavior; preserve recording stop/finalization guards.
- Repair video trim and timed text in preview and export. Use the supplied AFTER reference for a dominant preview, right layer list and bottom timeline with explicit In/Out controls. Do not imply unsupported multivideo/audio/color-grading features merely because the reference depicts them.
- Produce at least twelve concrete UI/UX and brand directions, each changing information hierarchy and tool placement as well as color and tone. Earlier artifacts/theme-2.1.0 palette studies are superseded as selection proposals. New proposals belong to artifacts/ux-2.1.0. Apply only the user's chosen direction. Direction choice is required before final UI implementation/release.
- Preserve user captures, existing settings and unrelated functionality.

## Approved appearance contract

Workspace is the new 2.1 default: light gallery and dark video editor use isolated window-local tokens/styles; shared application brushes must not bleed between these windows. Existing Daylight and HighContrast choices remain explicit options and are preserved. Legacy default Midnight (or empty/invalid legacy preference) is migrated once to Workspace using a small appearance revision marker; choosing Midnight afterward remains supported and persists. The old settings cannot distinguish deliberate Midnight selection from its shipped default, so this reversible one-time change is disclosed in release notes. No capture data migration is involved.

The earlier functional commit d41bbd5769d0a6e4eb15b15a5a89297b3299e6ab is pushed and its CI run 34743068198 succeeded. Final UI changes require their own exact-head checks before merge/release. Source/test author for this phase is workspace_implementation; lead owns documentation, integration and release.

## Ownership and checks

Lead owns baseline/version/integration/release and this record. export_implementation owns source/test changes. memory_review owns artifacts/theme-2.1.0 visual candidates only. agy-worker reads a dedicated frozen 2.0.0 checkout for independent video analysis. independent_review checks the final changes without editing source.

Use meaningful focused tests for selection, retention, recording lifecycle and trim/timed text contracts, a Release build, relevant video/UI checks, and required exact-head GitHub CI. Prior emergency-release test omissions are recorded in their own release records and are not evidence for this release. Do not claim unrun visual/device checks as passed. Current host requires escalation for Git metadata/network and writes outside this repository.

## Progress

- Baseline alignment: verified complete.
- Design: original palette-only candidates superseded. User accepted the recommendation after twelve UI/UX directions under artifacts/ux-2.1.0: direction 02 gallery + direction 03 video editor.
- Native implementation: Workspace theme, three-pane gallery, filters, thumbnail-reusing detail, selected-layer properties and localized copy implemented. Role-local resources and dynamic style tokens fix cross-window palette lookup; Korean localization encoding was verified in actual native renders. Full final validation in progress.
- Validation: Release build passed. Latest full suite: Core 402 passed, App 715 passed and 1 skipped (local symlink prerequisite); TRX evidence under artifacts/validation/2.1.0/tests. First run caught stale trim-seek test expectations. Later new layout checks initially included a hidden ComboBox TextBox; corrected to verify the actual In/Out inputs. Subsequent focused video/time/layout suite passed 22/22. Final parent-height layout correction compiled successfully and author ran EnglishLayoutTests 4/4; lead separately reruns these final layout checks and inspects real output.
- Real MP4 editor diagnostic passed: exact release seek and bounded seek queue confirmed; scrub pointer p95 0.107ms, UI cycle p99 45.296ms, exact seek p95 22.800ms. These are fixture measurements, not general performance guarantees.
- WPF UX fixture rendering passed. Lead visual inspection found and confirmed fixes for clipped In/Out values, compact layer details/buttons and a hidden compact status row. Final artifacts/validation/2.1.0/ux-review-final-status contains real 96/144/192 DPI fixture renders and layout coordinates. Compact root height is now 505 within a 521-high parent; status bottom is 513. Root height had incorrectly fed its own overflow back into the available-space calculation; budgeting against the parent viewport resolves it. Native pointer interaction and mixed-DPI crossing have not been manually verified.
- Packaging and publication: pending final validation and verified PR integration.

## Independent analysis

AGY run 66c359be-1c96-4ebd-9984-f8a0485c8dd1 completed successfully on the frozen 2.0.0 checkout with no source changes or policy/scope violations. Its actionable findings cover trim-drag preview feedback, missing exact release seek, viewport movement during trim, handle hit-test inconsistencies, and ambiguous text time domains. Lead inspection rejected the suggested renderer matrix-offset defect: its translation correctly maps the offset source box to explicit destination bounds. Text placement reset behavior remains a separate UI issue. Findings require code review and executable validation; worker success alone is not a test pass.

Independent recording review identified initial-versus-acquired desktop bounds mismatch during monitor reconfiguration; stable mixed-DPI/negative-origin arithmetic was not shown to be defective. User-specific Ctrl+Shift+X symptom clarification remains optional and pending.

Early independent read-only review approved date-group toggling, edit icon wiring, explicit retention labels, recording bounds mismatch rejection and asynchronous preparation settling. Video editor changes remain under implementation and are excluded from that approval. The dedicated clean 2.0.0 analysis checkout was verified and removed after AGY completion; no Git safe-directory or host security settings were changed.

Subsequent independent review approved video trim undo/document synchronization, free source seeking, retained-range playback/export and timed text preservation. Review identified a 24-hour formatting defect; SourceTimeInput now displays numeric seconds for >=24h with 24h+1ms and 25h roundtrip coverage, and the narrow correction was independently approved. Lead identified frozen ThemeService brush replacement losing Overlay.Dimmer opacity; CloneCurrentValue now preserves opacity/transforms, covered by real token-resource frozen/unfrozen tests and independently reviewed.

Final parent-viewport height correction independently approved. Lead independently executed EnglishLayoutTests: 4 passed, 0 failed/skipped, exit 0; evidence artifacts/validation/2.1.0/tests/layout-final.trx. Lead inspected the final compact 192 DPI PNG and JSON coordinates and confirmed status recovery. Final theme integration, exact-head GitHub CI, merge, clean-main packaging, tag and publication remain outstanding.

Approved-design phase independent review: no P1 found. One P2 identified lingering inline video when switching filters; the handler now closes the player before changing the filter, with a real fixture check for Source=null and collapsed panel. Reviewer approved migration/clone/draft preservation, role-local brush isolation, thumbnail reuse/virtualization and the final right-side property summary/dialog entry. Author targeted checks passed 48 Core and 2 App tests before the last UI/diagnostic refinements; lead owns final full Release validation.

## Final approved-design validation

Lead reran the complete Release build and tests after the last source/test mutation: build exit 0, Core 407 passed, App 718 passed and 1 local prerequisite skip (719 total). Hosted CI must independently pass the symlink test. Native WPF diagnostic MyCapture-ux-review-1zfk33d4.igo passed; lead inspected compact selected-item gallery and selected-layer editor renders. Gallery wheel/virtualization, 304-DIP card reachability, filter media release and keyboard focus checks passed. Raster density is not native mixed-DPI pointer validation. Narrow independent review of scroll reachability assertions and diagnostic selection timing found no remaining P1/P2. Branch CI, packaged validation, PR integration and release publication remain pending.

Final real MP4 responsiveness diagnostic passed using an absolute output path: exact seek p95 29.586 ms, bounded pending/in-flight seek checks and trim/zoom/Fit All usability passed. The preceding relative-path diagnostic failed media readiness; it is retained as failed evidence and was not counted as a pass. Final evidence: artifacts/validation/2.1.0/workspace-video-absolute.
