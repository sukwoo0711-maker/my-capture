# v1.8.0 performance, stability and editor completion

User-authorized scope: performance review and real before/after measurements; rapid repeated region capture; screenshot capture during recording and cancellation/close edge cases; one-click update; Korean/English preference; optional date/time including seconds and milliseconds in recording with a movable/resizable floating clock; visible video text and direct timeline duration/position manipulation; shape/image video layers; commercial video-editor UX research; GitHub push, verified PR integration and release.

Historical comparison source:21c12cbf5b791e9cde8e6833db0587044d1d59a3(v1.7.0). Another session integratedandpublished v1.7.1 viaPR7 atde6b8fdb6fc8bfd4b4c73dac41081b622687aa13 duringinitial audit. Currentimplementationbase isde6b8fd; isolatedworkbranch agent/v1.8.0-completion atartifacts/worktrees/v1.8.0. Preserve that patch and measure it rather than repeat assumedmissingchanges. The earlier v1.7.0 completion does not prove this new goal.

## Stable behavior and acceptance boundary

- Preserve existing capture, pin gestures, export compatibility, offline capture/OCR, private user captures, settings and annotations. Updating contacts GitHub only as part of the requested update flow; no telemetry.
- Support screenshot capture during recording without polluting the video with capture controls, corrupting files or losing recording state. Avoid replacing the requested concurrent scenario with blanket feature blocking. Busy editor/selection requests must be rejected or coalesced before expensive pixel work.
- Measure production call paths, not a toy substitute: pin move latency/CPU/allocations, idle with/without visible windows, repeated capture/persistence, OCR text/blank input, recording frame time/CPU/allocations/dropped frames, concurrent capture/recording and cancellation. Record environment, workload, process CPU normalization, sample count, medians/tails and limitations. Do not promise a speedup until measured.
- Optional clock is an explicit visible recording element with date/time/seconds/milliseconds, adjustable position/size, stable capture-coordinate mapping and bounded refresh rate. UI control overlays remain excluded from capture. Clock rendering must not restart continuous idle work when disabled.
- Video text, shapes and images must be visible in preview and exported MP4/GIF; selection/drag/resize and timeline move/trim share the persisted model, respect clip boundaries, and support keyboard/undo where existing architecture permits. Benchmark mainstream editor behavior using official sources.
- Update button checks the canonical repository stable release, downloads/validates exact expected artifacts, reports progress/errors/cancel, waits for safe application state, invokes the existing per-user transactional installer and restarts. Never discard active work or silently trust an arbitrary download URL.
- English option covers ordinary capture/pin/library/settings/recording/editor/update flows, persists selection, retains Korean default compatibility and avoids clipped or untranslated primary UI. No OS language changes.

## Work sequence and ownership

1. Freeze/read-only audit and production-path measurement harness. Run baseline before optimizing.
2. Pin drag hot path; direct reusable recording frame capture; background indexing/persistence and lifecycle coordination. Isolate changes and validate actual before/after measurements.
3. Video text rendering defect, shared direct-manipulation/timeline UX, shape/image layers and optional recording clock.
4. Update flow and language resources, with focused integration and failure-path verification.
5. Final full Release suite, actual UI/compound-scenario QA, seven packaged self-tests, installer security/rollback tests, clean exact source packaging, PR exact-head CI/security checks, verified integration, tag/release and redownloaded asset hashes.

Astra owns scope, investigation acceptance, baseline collection, integration, checks, commits and remote actions. Codex workers began read-only audits but all reached an account usage limit; their incomplete audits are hypotheses only. The available agy-worker is used for bounded implementation in disposable worktrees; its quota/host rules remain in force. No worker patch/test claim is trusted without lead inspection and host validation.

## Evidence at start

- Installed MyCapture PID73812 is version1.7.0.0. Three5-second read-only CPU samples:0ms processCPU each, ~476.8MB working set,1025handles,19threads,16logicalprocessors. This observation does not reproduce constant idle CPU usage or prove other scenarios healthy.
- Agy Analyze run7de8f041-5ee5-4c65-992d-45d6930090a0 completed successfully against clean baseline. Findings identify per-move display enumeration, repeated PNG OCR conversion/rotation work, capture work before busy rejection, duplicate original PNG encoding, synchronous queue persistence, recording/screenshot overlay-state risks and per-frame GDI/WPF/LOH copies. Cost estimates and proposed fixes require measurement; possible cancellation races require source/runtime verification.
- No implementation or deployment for this new goal has been completed yet.

## Reconciled concurrent session

PR7 changed28files andpublishedv1.7.1. The currentbaselineRelease suite passes754tests(358Core396App),0warnings/errors. It blocks all screenshots during recording; this does not satisfy the full concurrentcapture scenario and must be replaced by safe coexistence. Its live drag/concurrentcapture measurements were explicitly not run. No claim that the full newgoal is fulfilled follows fromPR7. Historical1.7.0 andcurrent1.7.1 must be distinguished inbefore/afterdata.


## Verified progress and review candidates (2026-09-08)

- Main task branch 26020e98fb5d70f4175cf2ccad90e3cdc8431d55 includes reviewed subtitle fix (2ae4b05) and GDI readback contract fix (26020e9). Text worker full preexisting Release suite: 754 passed. New pixel regression: baseline 18 failed/1 passed; corrected build 19 passed. Native synthetic editor confirms Hangul text inside background. GDI independent capture/recording tests: 29 passed.
- Real packaged 1.7.0/1.7.1 comparison and current instrumented recording findings are recorded under ignored artifacts/validation/v1.8.0. Short tests previously returned PASS with only 1-2 frames; do not use them as target-rate proof. Instrumented 320x240 sessions show ~1s encoder initialization, ~21ms average capture, much smaller synchronous encoding. No sustained high-resolution speedup claim yet.
- Video candidate branch agent/video-review-v1.8.0 at 7b14061 includes timeline rows/body+edge drag and recording metrics candidates. Timeline passes 68 video-related tests but needs drag hot-path refinement (repeated list rebuild / frame image decoding). Metrics compile and diagnostic pass; remove redundant aliases before final acceptance. Do not deploy review candidate checkpoints.
- Update candidate branch agent/updater-review-v1.8.0 at 5affd6f holds initial service-only foundation. Compile succeeds, but lead found unbounded API response, overly broad initial URL acceptance, checksum ambiguity, weak staging cleanup and per-chunk WriteThrough. Dedicated refinement worker is running before acceptance. No update UI or install/restart handoff exists yet.
- Concurrent capture worker 0a1a6a8d-08b5-4546-b3c8-6a5f60cef601 succeeded wrapper validation. Independent tests pending. Lead found editor exclusion failure is currently ignored and prepared-recording/countdown guards need refinement. Main branch does not contain this candidate yet.
- Worker status details: initial measurement harness scope_violation 479f4a9e was not accepted or reused; comma OwnedPath list became one CLI argument. Use a single prefix. Internal worker-state path cannot be a Repository target (invalid_repository e44caabf); dedicated ordinary repo review worktree fixes this preflight layout without permission expansion. Wrapper max concurrency is 2; concurrency_limit35c6f746 started no worker, and work proceeded only after a slot was free. Codex worker account quota remains unavailable; do not retry/switch models to evade it.
- Astra still performs planning/review/integration/verification, with worker implementation. Earlier optional request to relax role separation is not required to continue the valid delegated workflow.

Remaining completion gates unchanged: measured performance/stability including actual drag and concurrent capture, accurate recording startup, optional movable/resizable timestamp clock, English UI, text/shape/image spatial editing and persisted/exported rendering, complete one-click updater, UI review, final tests/package/installer/PR/integration/release. No v1.8.0 remote action or release has occurred.

## Independent review checkpoint (2026-09-08 evening)

- Pushed agent/v1.8.0-completion at b7d56ffdff802187500d7a86fafe4d4f6132648a and opened Draft PR #8: https://github.com/sukwoo0711-maker/my-capture/pull/8. Full Release suite 773 passed; exact-head GitHub Release build/tests (push and PR) and CodeQL succeeded. No merge/tag/release. Optional separate issue creation was rejected by automatic approval review; user clarification is pending, and code work continues without an issue.
- Updater candidate is local review branch 85026f8. Worker 77e8806b passed wrapper validation; independent focused tests 31 passed, 1 failed: declared metadata >1MiB returns InvalidReleaseData, whereas streaming limit and test expect PayloadTooLarge. Download safety tests are not proof of installation/restart. UI/check/download integration and classification correction delegated next.
- Clock candidate is local review branch 2c414e6. Worker ab20fecd passed wrapper validation, but independent build fails CS0115 (invalid WPF DPI override). Do not release. Correction, small-region fit and lifecycle tests delegated. Disabled state creates no clock/timer; actual recorded-pixel inclusion still needs verification.
- Timeline worker f6c0e60e passed wrapper validation; independent tests 6 passed, 1 failed (preview synthetic red pixel remained zero). Mutable-list height snapshot fix is plausible, but cache expansion also decodes all uncached inactive frames before deciding admission, risking repeated decode churn over budget. Keep this candidate out of final integration until corrected; do not treat its retention budget as a hard memory bound for active layers.
- Concurrent refinement 89143110 passed 29 independent related tests. Still review its generic error balloon: it incorrectly promises capture was saved even when failure may occur before persistence, and mentions recording for unrelated transition errors. Its clock-control overlap must be resolved during integration.
- Existing recording measurement isolates approximately 1 second encoder initialization and approximately 21ms capture per 320x240 frame. Recorder stopwatch currently starts before initialization; frame timestamps/drop statistics must be corrected before using reported FPS as steady-state evidence. Reusable capture resources remain a candidate optimization requiring before/after measurements.

## Accepted integration and remaining blockers (2026-09-08 20:25 KST)

- Clock: d8cf509d passed wrapper validation and 17 independent STA tests. Integrated as be9ac21; main task build and the same 17 tests passed. Review branch checkpoint 82fb241. Actual clock-in-video and mouse/DPI hardware verification remain pending; do not infer these from unit tests.
- Timeline: native synthetic fixture from 0f19af84 confirmed body drag 0.300-2.000 seconds to 0.179-1.879 (duration preserved), end trim to 1.508, and visible Hangul at playhead 0.400. At 770x555 the layer tools are below the scroll viewport; improve discoverability before final release. Full integrated Release suite passed 790 (358 Core + 432 App) before the final isolated geometry fix. Geometry-only source and two previously passing worker tests from f6c0e60e are included separately; cache regression tests/candidate are not represented as accepted.
- c99beb0e cache-demand candidate passed 8 independent tests, but native 770x555 fixture showed absent active caption at 0.400 seconds, including a settled second screenshot. New MeasureOverride returning full canvas dimensions is suspect. This candidate remains excluded. Earlier 0f cache reuse is integrated; negative decode cache/soft byte budget still need completion.
- Updater UI f4118404 and recording-performance 0a24704e both failed wrapper scope validation: worker-reported changed_files did not match actual Git state. Neither result is accepted, copied or retried. Scope errors are separate from host automatic approval review. One-click update UI/install/restart and the measured recording performance/startup changes remain incomplete.
- Latest worker quota: five-hour 3%, resets 23:46 local; new worker runs blocked at <=10%; overages disabled and no model-pool fallback. Existing Codex workers remain account-quota blocked. A user question to allow Astra direct implementation is pending; no response has been inferred. Continue review/test/integration within lead role.
- Native fixture processes are synthetic only: PID59712 (0f fixture, 600-second hold) and PID77848 (c99 fixture, 300-second hold). No user captures or unrelated app data were published. Old fixture video window was closed after review; remaining fixture windows expire automatically.
- Windows execution runner twice failed new-process pipe connection; subsequent commands succeeded with login disabled. First clock review commit was separately confirmed; failed main copy commands had not run. Do not confuse a runner timeout with an approval denial or successful mutation.
- No v1.8.0 release exists. English UI, one-click updater, shape/image spatial editing, actual concurrent capture QA, recording clock inclusion, comprehensive performance evidence and final packaging/installer/release gates remain open.

## Resumed with available subagents (2026-09-08)

User instruction: continue; when Antigravity is unavailable, Astra invokes subagent models. A fresh GPT-6 Astra read-only subagent audit succeeded with no quota error against clean6da612e. This replaces the earlier assumption that all Codex subagent calls remain unavailable. Lead retains planning, review, integration, independent verification and remote release; implementation stays delegated. Antigravity quota gates remain unchanged; rejected outputs are not reused.

Active ownership:
- update_completion: agent/updater-review-v1.8.0 at85026f8; verified committed updater foundation, complete UI + safe installer handoff/receipt/restart and focused tests. App.xaml.cs edits limited to update wiring.
- video_completion: agent/video-features-v1.8.0 at6da612e; video text/shape/image normalized spatial bounds and direct manipulation, shared preview/export, timeline/tool discoverability, constrained-parent rendering tests.
- resume_audit: agent/capture-performance-v1.8.0 at6da612e; recording readiness/timebase/metrics, session native resources, concurrent still capture exclusions/state guards, meaningful capture diagnostics/tests. App.xaml.cs edits limited to capture/recording wiring.

English UI localization follows integration of these independently developed surfaces to avoid translating obsolete intermediate UI. Final pin/idle/rapid-capture profiling and original13-item regression audit remain within the full goal. No new release or merge has occurred.

## Integrated feature checkpoints and review (2026-09-08, late session)

- Updater worker checkpoint06abf3d plus reviewed foundation commits integrated as ba4fdb1/dde2f11/e948194/de4b4c9. Lead inspected installer session/receipt/restart validation and independently ran the fake-process helper harness:9 cases passed. Real packaged installer receipt/restart remains a release gate. Followup adds validated custom-root updates and clearly labeled portable-to-per-user installation, then automatic download-to-install continuation when no work is at risk.
- Video checkpointc391177 integrated as470e80e. Lead reviewed normalized spatial bounds, preview/export compositor, demand/negative image cache, pinned tools, undo/redo and playback/seek lifecycle. Worker reported native App73 and Core20 passing. Aggregate decoded/encoded document memory limits are a required followup; the inactive cache budget alone is not an overall memory bound.
- Recording checkpointd87f80d and corrected5372bab integrated as d98e0d1/55957c8. Lead review found and required a separate capture-exclusion predicate through recorder stop, a preserved legacy pixel comparison buffer, and actual encoder-factory thread assertions. Worker also corrected dispatcher affinity after asynchronous capture persistence. Worker focused35 passed.
- Independent integrated validation at55957c8: Release App filter Update/VideoSpatial/VideoComposition/RecordingCaptureLifecycle/TimedText passed92, failed0, skipped0; audit remained enabled and native API tests ran outside the workspace sandbox. Log: artifacts/validation/v1.8.0/integrated-features-tests.log. This is focused integration evidence, not final full-solution or packaged validation.
- Synthetic recording diagnostics encountered a decoder seek HRESULT and a separate encoder-readiness timeout. These are retained failures under investigation, not successful throughput or clock-in-video proof. No final performance improvement claim or release is made at this checkpoint.
- Prepared isolated localization branch agent/localization-v1.8.0 at55957c8; reuse existing GeneralSettings.Language and merge followup UI strings before final localization acceptance.

## Followup integration, native review and CI findings

- Integrated updater custom/portable continuation b8dbdf9 as72f0fbd, video resource guard7f0cc2c as a3bc296, and recording diagnosticb0f9df2 as d8c847b. Independent PowerShell helper14 cases and preservation/root/collision/junction cases passed. Actual packaged install/restart remains pending.
- Native lead review of the integrated editor confirmed actual pointer movement and corner resizing of a newly added rectangle, plus visible Hangul and pointer movement of the text layer. Shape view bounds moved from325,94–440,180 to373,117–489,203, then resized to514,214. Text artwork and selection moved together. These synthetic windows used no user media and were closed after review (PID62108).
- The same review rejected minimum-window timeline clipping and excessive maximized blank space. Worker compact followupbcefa0e integrated as6abf1f9; independent native full-theme review confirmed overview/detail tracks and the text timing bar visible at766×553 screenshot size, with tools/status inside the window. At maximized3439×1392, spare height expands the preview. An intermediate screenshot was captured during the Windows maximize animation; the settled screenshot was used for acceptance. All five synthetic windows fromPID93288 were closed.
- Independent followup Release App tests at6abf1f9 passed71, failed0, skipped0. Log: artifacts/validation/v1.8.0/integrated-followup-tests.log.
- Independent actual recording diagnostic at6abf1f9 completed exit0 and RESULT:PASS with the final dispatcher-pumping harness. MP4 decoded probe counts: plain0, excluded selector0, excluded editor0, included clock5130. Sustained320:149frames/6044.454ms=24.651fps;1280:126/6058.694ms=20.797fps; concurrent123/5473.227ms=22.473fps. Capture wall time dominates encoding. These establish correct recorded pixels and bounded test completion, not a30fps or latency-speedup claim. Raw data: artifacts/validation/v1.8.0/recording-integrated-final/. Earlier worker raw runs were copied alongside it; failures retained.
- Pin lifecycle776da73 integrated as39d2d8b after lead review: actual input capture failure, loss, button release and close end the gesture and clear the desktop cache. Worker focused23 passed. Synthetic real-callback drag/idle and persistence/OCR cost measurement continues; no wholesale asynchronous queue migration is accepted.
- Pushed083ff838b2349704c1392200fe769e09995c29ed to existing Draft PR#8 and updated its body. Both Release build/test runs passed. CodeQL language analyses completed, but umbrella check102096055628 failed with34 new alerts (path handling in updater/test fixtures, command executable source, log input). This is an open release blocker, not an accepted pass. Exact annotations retained in artifacts/validation/v1.8.0/codeql-083-annotations.json. A separate security worker branch starts at6abf1f9; no rule exclusions or blanket dismissals authorized.
- Localization worker now owns agent/localization-v1.8.0 fromd8c847b plus compact followup, translating explicit resource callsites across ordinary product surfaces. Security worker owns structural updater/path changes; pin worker owns drag/persistence diagnostics. Final merge/test/version/package/release gates remain open.

## Ordered publication and security verification (2026-09-09)

- Accepted c0e65ec as9e87635 after reviewing immutable owner-thread serialization, FIFO disk jobs, bounded async reservations, sync drain without dispatcher deadlock, and eviction/revision protection. Independent integrated tests passed3Core and5App,0failed/skipped. Log: artifacts/validation/v1.8.0/integrated-async-publication-tests.log. This is correctness evidence, not speedup proof.
- Accepted eac47b8 as ea641e4941a98cd91e0fa4bda41dccc0d1e1f322: diagnostic CLI output placement is retained with canonical containment and link rejection. Independent integrated13tests passed0failed/skipped (integrated-diagnostic-path-tests.log). This exact head was pushed. CodeQL umbrella102118718353 succeeded with0annotations/no new alerts; actions102118527744 and csharp102118527483 succeeded. Earlier34updater and8diagnostic alerts are closed at this SHA.
- PR CI34243245748 built successfully; App545/546 passed. Sole failure: VideoEditorWindowTests.TwoSecondClip line235 assumes requested900DIP height always yields fixed preview growth despite CI work-area limits. Localization worker owns actual-client allocation assertions and UI fixes, with no blanket skips.
- Lead inspected production-themed English settings780, video760 and timed-text images. Settings and text dialog were readable. Video overview hint and bottom status were clipped;10.5pt text occupied8DIP. Worker must fit readable hint/status in the actual visible client before acceptance.
- First after-publication sample wrote RESULT:FAIL: OCR publication failed, followed by File.Replace IOException in AtomicFile.cs129 during Queue.Save. Raw sample preserved in capture-performance worktree at artifacts/validation/pin-storage-after-ordered-publication. It overlapped a build and runner outages, but causation is unproven. Original batches showed7.4–7.9s wall medians versus baseline milliseconds; no speedup inference. Valid32-record index/backup remained, no stale root temporary file, ~802GiB free. Diagnostic-only HRESULT/stage logging and an uncontended rerun are pending.
- Windows runner intermittently delayed sessions or failed pipe-in; GitHub connector independently retrieved logs/checks. Collect pending mutations before retries; tool delay is not app-hang proof. English final integration, actual packaged update/restart, full checks, clean-main packaging and release remain open.

## Final integrated source verification

- Localization7022d2f integrated asaa11716; confined diagnostic83a3802 as5147ca9. Lead accepted corrected production-themed760px English editor: complete overview hint, layer/action rows and bottom status. Settings780 and520px timed-text dialog also inspected.724 resource pairs retain user text and persisted data. Shared path helper remains intact.
- Quiet publication evidence0a47242 integrated as228276d. Root independently read the PASS report/raw measurements and preserved both successful and failed samples under artifacts/validation/v1.8.0/. OCR UI service medians13.320/15.039/17.770ms became0.232/0.940/1.121ms; awaited wall time did not improve. Original after service includes a broader interval; no direct ratio claim. File.Replace failure did not recur and remains unexplained, not claimed fixed.
- Final requirement review found late OCR feedback could restart a closed pin's timer after its stop handler was detached.2291cf3 integrated asffa81ea adds a central closed guard and two delayed-completion regressions; worker27pin tests passed. The thirteen original requirements and added workflows have code/test/native evidence; exhaustive physical mixed-DPI hardware and guaranteed30fps are not claimed.
- Full integrated source test at5147ca9 built successfully but had374Core/547App passes and one invalid symmetric process-wide GDI-count assertion (-12 cleanup delta).32c8f66 integrated as5100139 retains the+2growth ceiling and original pixel/error/disposal checks while permitting unrelated resource release. A proposed GetObjectType deletion probe was discarded after direct native control reproduced a nonzero type despite successful DeleteDC. No production workaround was introduced.
- Final required Release build/test at5100139 exited0:374Core+550App=924passed,0failed/skipped,0warnings/errors. Logs: artifacts/validation/v1.8.0/final-release-tests.log and build/logs/{build,test}.log. Independent update-restart preflight accepted valid synthetic inventory and rejected wrong source, dirty source, tampered setup and bootstrap mismatch; no install occurred in preflight.
- Release metadata is1.8.0/1.8.0.0; prepared actual packaged update/receipt/binary diagnostic-restart harness and metadata-only embedded-helper reader are integrated. Exact-head CI, packaged diagnostics, actual installer acceptance and clean-origin/main release provenance remain gates before publication. Remote ruleset requires linear history and squash/rebase PR integration, with strict Release build and tests and no bypass.

## Candidate package acceptance and remaining release gate

- Clean candidate0ffa0f2ee7a20d4ace74c234f2b209069445ff78 packaged successfully:267payload files,1.8.0, self-contained win-x64. Actual packaged IExpress update and installed-binary diagnostic restart exited0/PASS. Lead read acceptance-result.json and exact successful installer receipt. Evidence: artifacts/validation/v1.8.0/candidate-update-restart.log and artifacts/validation/update-restart/9936cd248e6d4bac927d00ed99745b60/. This does not claim normal resident startup.
- Candidate hostile installer script exited0:18passes across11invocations, including corruption/traversal/duplicate rejection, disk and mutex failures, owned Unicode target, exact receipt, preserved unmanifested user file, rollback after backup/commit, and safe uninstall retaining external data/Run state. Log: artifacts/validation/v1.8.0/candidate-installer-hostile.log.
- Both0ffa0f2 remote Release jobs passed. CodeQL reported5new localization fixture path flows;83cff34 integrated as9c896d8 changes only EnglishLayoutTests/LocalizationTests to reuse owned roots and validated screenshot children. Product SettingsStore is unchanged. Independent integrated21Core+15App focused tests passed with actual English screenshot output. No suppressions or skipped tests. Exact final-head CodeQL must clear before merge/release.
- The complete seven-test packaged runner requires no resident MyCapture because it exercises real default global hotkeys. Existing user installation PID154900 was observed at LocalAppData/Programs/MyCapture/MyCapture.exe, with no exposed MyCapture window. It has not been force-stopped. User was asked to close the tray app after preserving work; no answer or closure is inferred. Restore that same installation after the tests. Main merge/tag/final clean-main packaging/publication remain unperformed until gates complete.
- One push was rejected by automatic review requesting destination ownership proof. Read-only git remote/gh repo view confirmed the existing public sukwoo0711-maker/my-capture destination and ADMIN permission; retry of the same authorized branch push succeeded without a destination change. The connector's PR-write403 was handled through the existing authorized gh client, not permission expansion.
